<input
  value="{{value}}"
  type="{{inputType}}"
  placeholder="{{placeholder}}"
  disabled={{isDisabled}}
  readonly={{isReadonly}}
  maxlength={{maxLength}}
  onKeyUp={{action onChange value='target.value'}}
/>


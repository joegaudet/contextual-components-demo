<input
  type="{{inputType}}"
  placeholder="{{placeholder}}"
  value="{{readonly value}}"
  disabled={{isDisabled}}
  readonly={{isReadonly}}
  maxlength={{maxLength}}
  onKeyUp={{action onChange value='target.value'}}
/>

